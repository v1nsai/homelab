apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: open-webui-data
  name: open-webui-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
