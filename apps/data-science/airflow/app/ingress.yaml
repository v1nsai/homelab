apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: airflow
spec:
  rules:
    - host: airflow.internal
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: airflow
                port:
                  number: 3000
