apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: overseerr-config
  name: overseerr-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
