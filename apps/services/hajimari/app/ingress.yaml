apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hajimari-ingress
spec:
  rules:
    - host: hajimari.internal
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: hajimari
                port:
                  number: 3000